#!/usr/bin/env python3
# creating function that return list of lists of integers in pascal triangle

import pascal_triangle

def pascal_triangle(n):
  for( x, prev, next ) in n:
  x = [1, n]:
  if( x = 1, x <= 5, x++ ):
      print([1, (prev + next)]):
else:
    print([])
